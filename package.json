{
  "name": "european-dream-website",
  "version": "1.0.0",
  "description": "European Dream website - Hugo static site",
  "type": "module",
  "scripts": {
    "fonts:copy": "cp node_modules/@fontsource-variable/jost/files/jost-*-wght-normal.woff2 static/fonts/",
    "scss:build": "sass assets/scss/index.scss:static/css/main.css --load-path=node_modules --style=compressed --no-source-map",
    "scss:watch": "sass assets/scss/index.scss:static/css/main.css --load-path=node_modules --watch",
    "js:build": "bun build assets/js/index.ts --outfile=static/js/main.js --minify",
    "dev": "bun run fonts:copy && bun run scss:build && bun run js:build && hugo server --buildDrafts --buildFuture -p 3000",
    "build": "bun run fonts:copy && bun run scss:build && bun run js:build && hugo --minify",
    "serve:example": "bunx serve -p 8080 example",
    "test:watch": "bun test --watch assets/js/",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:unit": "bun test assets/js/"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.11.0",
    "@fontsource-variable/jost": "5.2.8",
    "@playwright/test": "1.57.0",
    "@types/bun": "1.3.6",
    "bootstrap": "5.3.8",
    "sass": "1.97.2",
    "typescript": "5.9.3"
  },
  "license": "EUPL-1.2"
}
